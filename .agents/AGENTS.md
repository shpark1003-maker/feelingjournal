# Project Rules

- **API Key Exposure Prevention**: NEVER display, print, or output raw API keys, credentials, or secrets in chat responses, logs, or artifacts. If an API key needs to be referenced, use placeholder formats (e.g., `YOUR_API_KEY`, `<API_KEY>`) or refer to it by the variable name (e.g., `GEMINI_API_KEY`).
